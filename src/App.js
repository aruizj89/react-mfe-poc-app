import { Card, Container } from "@mui/material";
import React from "react";

const App = () => (
  <Container sx={{ p: 3 }}>
    <Card sx={{ p: 3 }}>React MFE POC App</Card>
  </Container>
);

export default App;
